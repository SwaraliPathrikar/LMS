You are an expert full-stack developer.

Treat this task as a **production hotfix**, NOT a redesign, NOT a refactor, and NOT a UI improvement.

I will provide an existing working Library Management System (LMS). The system is already complete and functional.

---

## 🚨 NON-NEGOTIABLE RULES (READ CAREFULLY)

If you violate ANY of these, the solution is INVALID:

* DO NOT redesign UI
* DO NOT change layout
* DO NOT change styling (CSS)
* DO NOT modify existing HTML structure
* DO NOT refactor code
* DO NOT rename files
* DO NOT replace forms
* DO NOT create new pages for this feature

👉 You are ONLY allowed to ADD minimal code.

👉 Think like you are patching a live production system.

---

## 🎯 OBJECTIVE

Add **barcode-based book issuing** inside the existing Librarian → Issue Book workflow.

---

## 📍 EXACT INTEGRATION POINT

You MUST locate the existing **Issue Book form/page**.

STRICT REQUIREMENTS:

* Use the SAME existing form
* DO NOT recreate the form
* DO NOT change form fields
* DO NOT change layout of the form

---

## ➕ ALLOWED CHANGES (ONLY THESE)

### ✅ 1. Add ONE button inside existing Issue Book form

Label:
"📷 Scan Book Barcode"

Placement:
👉 NEXT TO existing "Book ID" input field

DO NOT disturb layout.

---

### ✅ 2. Add scanner functionality (NO UI CHANGE)

Use: html5-qrcode (free browser-based library)

Behavior:

* On button click → open camera scanner
* Scanner must appear as:
  👉 overlay / modal ONLY
* DO NOT push or resize existing layout

---

### ✅ 3. Barcode Scan Flow

After scanning:

1. Extract barcode value
2. Call existing backend:

GET /api/book/{barcode}

3. Use response to AUTO-FILL EXISTING FORM FIELDS:

* Book ID → barcode
* Book Title → from API
* Availability → show status

👉 DO NOT create new input fields
👉 ONLY fill existing inputs

---

### ✅ 4. Validation (Minimal UI impact)

* If book unavailable:

  * Show simple text message near field
  * Disable Issue button

* If available:

  * Allow normal flow

---

### ✅ 5. Issue Book Action

DO NOT change existing issue logic.

Ensure:

* Scanned barcode value is used in submission

---

## 📚 REAL-WORLD FLOW (MANDATORY BEHAVIOR)

At library counter:

1. Librarian opens Issue Book page
2. Selects Student
3. Clicks "Scan Book Barcode"
4. Scans book
5. Form auto-fills
6. Clicks "Issue Book"

---

## 📱 MOBILE SUPPORT (SAFE ONLY)

* Add ONLY minimal media queries
* DO NOT change desktop UI
* DO NOT restructure layout
* Only ensure:

  * Buttons are clickable on mobile
  * Scanner works properly

---

## 🚀 PWA SUPPORT (ADD-ONLY)

Create NEW files only:

* manifest.json
* service-worker.js

Link them with minimal changes.

DO NOT alter main HTML structure.

---

## 📁 FILE RULES

ONLY ADD:

* scanner.js
* (optional) scanner.css
* manifest.json
* service-worker.js

DO NOT rewrite existing files.

ONLY allowed edits:

* Add ONE button
* Add ONE script tag

---

## 📤 OUTPUT REQUIREMENTS (VERY STRICT)

Provide ONLY:

1. Exact code snippet to ADD button (with placement hint)
2. Script tag to include library
3. scanner.js file
4. Minimal instructions (where to paste code)

❌ DO NOT output full rewritten files
❌ DO NOT output redesigned UI

---

## 🚫 FAILURE CONDITIONS

If your solution includes ANY of the following, it is WRONG:

* Changed layout
* Modified existing CSS
* New page for scanning
* Rewritten form
* Different UI

---

## 🎯 FINAL EXPECTATION

After integration:

* Existing LMS looks EXACTLY the same
* Only one new button is visible
* Scanning works
* Form auto-fills
* Issue flow remains unchanged

---

This is a strict extension task. Not a redesign.

---
