[build]
  base    = "artifacts/lms"
  command = "pnpm install --ignore-scripts && pnpm run build"
  publish = "artifacts/lms/dist/public"

[build.environment]
  BASE_PATH    = "/"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
